{
  "extends": [
    "next/core-web-vitals",
    "next/typescript"
  ],
  "rules": {
    "@typescript-eslint/no-unused-vars": "off", // Disable unused variables check
    "react/prop-types": "off", // Disable prop-types (not needed with TypeScript)
    "no-console": "off" // Allow console logs but warn instead of error
  }
}